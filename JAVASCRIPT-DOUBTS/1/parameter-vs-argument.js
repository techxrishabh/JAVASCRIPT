// WHAT IS THE DIFFERENCE BETWEEN PARAMETER AND ARGUMENTS ?

//__________________________ANSWER_________________________________;

// function add (parameter1 , parameter2 ) {
//   return parameter1 + parameter2;
// }

// add(10 (argument 1), 20 (argument2 ))

// parameter is a variable passed to a function
// The arguments are the actual values to variable that we pass in the function

function addSum(a, b) {
  return a + b;
}

console.log(addSum(10, 20));
